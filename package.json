{
  "name": "wp-boilerplate-theme",
  "type": "module",
  "scripts": {
    "dev": "pnpm dlx gulp watch",
    "build": "pnpm dlx gulp build",
    "build:styles": "pnpm dlx gulp build:styles",
    "build:scripts": "pnpm dlx gulp build:scripts",
    "build:images": "pnpm dlx gulp build:images"
  },
  "devDependencies": {
    "browser-sync": "^3.0.4",
    "gulp": "^5.0.0",
    "gulp-autoprefixer": "^9.0.0",
    "gulp-imagemin": "^9.1.0",
    "gulp-rename": "^2.0.0",
    "gulp-sass": "^6.0.1",
    "gulp-uglify": "^3.0.2",
    "sass": "^1.87.0"
  }
}